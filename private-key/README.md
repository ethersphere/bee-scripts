Prints the private key of a node's swarm/ethereum key

### Usage

```
 go run main.go --data-dir="./clusters/bee-storage/bee-0" --password="~~~"
```

`--data-dir` points to your node's data directory. 
